lista = [1, 2, 3, 4, 5] #Cria uma lista

tupla = (1, 2, 3, 4, 5) # cria uma tupla

set = {5, 5, 21, 40} # Cria um conjunto

lista.append(6) #Adiciona elementos na lista

print(len(lista)) #Verifica a quantidade de itens na lista
print(len(set)) #Verifica a quantidade de itens distintos no conjunto